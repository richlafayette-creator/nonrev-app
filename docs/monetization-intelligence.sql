-- Monetization and intelligence production-readiness notes
-- Safe scaffold only: no Stripe secret keys, webhooks, or live charge paths are configured here.

-- 1) Credit ledger scaffold. Prefer append-only accounting over mutable balances.
-- create table if not exists public.credit_ledger (
--   id bigint generated by default as identity primary key,
--   user_id uuid references auth.users(id) on delete cascade,
--   event_type text not null check (event_type in ('purchase', 'request_hold', 'request_settle', 'refund', 'responder_reward')),
--   credits_delta integer not null,
--   stripe_checkout_session_id text,
--   load_request_id bigint references public.load_requests(id) on delete set null,
--   load_response_id bigint references public.load_responses(id) on delete set null,
--   notes text,
--   created_at timestamptz not null default now()
-- );

-- 2) Stripe product mapping scaffold. Store lookup keys, not secrets.
-- create table if not exists public.credit_products (
--   id text primary key,
--   stripe_lookup_key text not null unique,
--   credits integer not null,
--   active boolean not null default true,
--   created_at timestamptz not null default now()
-- );

-- 3) Intelligence snapshots scaffold for delay risk and itinerary ranking audits.
-- create table if not exists public.flight_intelligence_snapshots (
--   id bigint generated by default as identity primary key,
--   flight_id bigint references public.flights(id) on delete cascade,
--   delay_risk_score integer not null,
--   delay_risk_label text not null,
--   reasons jsonb not null default '[]'::jsonb,
--   created_at timestamptz not null default now()
-- );

-- 4) Notification trigger target scaffold. Use a server-side function later so client subscriptions are additive, not authoritative.
-- create table if not exists public.notifications (
--   id bigint generated by default as identity primary key,
--   user_id uuid references auth.users(id) on delete cascade,
--   topic text not null,
--   title text not null,
--   body text not null,
--   read_at timestamptz,
--   created_at timestamptz not null default now()
-- );
