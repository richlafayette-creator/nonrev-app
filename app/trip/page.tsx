'use client'  import { useState } from 'react'  export default function TripPage() {   const [query, setQuery] = useState('')   const [result, setResult] = useState('')    function searchTrip() {     const match = query.toUpperCase().match(/([A-Z]{3})\s+TO\s+([A-Z]{3})/)     if (match === null) {       setResult('Try typing: LAX to CDG tomorrow')       return     }     setResult()   }    return (     <main style={{ padding: 40 }}>       <h1>AI Trip Search</h1>       <input value={query} onChange={(e) => setQuery(e.target.value)} placeholder='LAX to CDG tomorrow' style={{ padding: 12, width: 300 }} />       <button onClick={searchTrip} style={{ padding: 12, marginLeft: 8 }}>Search</button>       <h2>{result}</h2>     </main>   ) } 